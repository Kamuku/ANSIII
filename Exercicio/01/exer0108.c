#include <stdio.h>
#include <locale.h>

void main()
{
    setlocale(LC_ALL, "Portuguese");
    puts("Por que razão não se utilizou a linha \"#include <stdio.h>\" no programa 0101.c ?");
    puts("A linha \"#include <stdio.h>\" não foi utilizada no programa 0101.c porque o programa não fazia nada.");
}
