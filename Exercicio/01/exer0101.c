#include <stdio.h>
#include <locale.h>

void main()
{
    setlocale(LC_ALL, "Portuguese");
    puts("Qual é a função que não pode faltar em todos os programas?");
    puts("A função que não pode faltar em todos os  programas é a função main.");
}
