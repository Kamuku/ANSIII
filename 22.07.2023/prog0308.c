#include <stdio.h>

void main()
{
    int a,b;
    printf("Introduza dois numeros:");
    scanf("%d%d",&a,&b);

    if(a>=0)
        {
            if(b>10)
        printf("B e muito grande\n");
        }
    else
        printf("A tem o valor negativo");
}
